import React from "react";

export default function Foo() {
  return (
    <div intent="karma">
      <Button className="untouched">
        <span>×</span>
        Button
      </Button>

      <span></span>
    </div>
  );
}
